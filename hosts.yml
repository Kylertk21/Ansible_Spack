all:
  children:
    local:
      hosts:
        localhost:

    compute:
      vars:
        ansible_user: root
        ansible_ssh_private_key_file: /home/control/.ssh/cluster
      hosts:
        compute2:
        compute3:
        compute4:
        compute5:
